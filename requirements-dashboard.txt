# Dashboard container deps (Streamlit UI). Installed by the hpe_dashboard service.
# Deliberately excludes scikit-learn/scipy/joblib — the dashboard reads parquet +
# Postgres and never trains/loads models, so those heavy ML wheels are not needed.
# (networkx is pulled transitively by pyvis for graph rendering.)
-r requirements-base.txt
streamlit==1.58.0
plotly==6.8.0              # charts (timeline / host health)
pyvis==0.3.2              # correlation graph rendering (dashboard.components.graph_view)
google-generativeai==0.8.6  # Gemini incident summaries (dashboard.llm_summary)

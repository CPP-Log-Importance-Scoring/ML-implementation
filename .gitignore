.env
# Python cache
__pycache__/
*.pyc

# Virtual environment
venv/
.venv/

# VS Code (optional)
.vscode/

# Data files (future) — anchored to repo root so dashboard/data/ stays tracked
/data/
*.log
*.parquet

test_env_handler.py

CLAUDE.md
AGENTS.md   

test_summary.py

evaluation/results/oracle_report.txt

# Model artifacts (regenerable). Whole dir, recursively — the old
# ml/model_store/*.pkl globs were non-recursive and missed _archive/**,
# which let ~70 model binaries leak into `git add -A`.
ml/model_store/

.claude
docs/

# Local dashboard UI state — rewritten (key order only) on every dashboard
# session by dashboard/ui.py:persist_filters(), not meaningful source content.
storage/filters.json

# Core dependencies — pinned to the known-good versions validated in the local
# venv. Exact pins keep the Docker build reproducible and prevent silent major-
# version drift (e.g. elasticsearch 9.x rejecting the 8.11 server, pandas 3.x
# removing Styler.applymap). Bump deliberately, not implicitly.
python-dotenv==1.2.2
sqlalchemy==2.0.49
pandas==3.0.2
psycopg2-binary==2.9.12
pytest==9.0.3
scikit-learn==1.8.0
numpy==2.4.4
joblib==1.5.3
scipy==1.17.1
pyarrow==24.0.0
networkx==3.6.1
python-dateutil==2.9.0.post0
streamlit==1.58.0
pyvis==0.3.2
plotly==6.8.0
# Must stay <9 to match the Elasticsearch 8.11 server (docker-compose.yml).
elasticsearch==8.19.3
google-generativeai==0.8.6

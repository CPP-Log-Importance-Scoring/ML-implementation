# Shared runtime deps — imported by BOTH the pipeline and the dashboard.
# Pinned to known-good versions validated in the local venv. Bump deliberately.
pandas==3.0.2
numpy==2.4.4
pyarrow==24.0.0              # parquet engine used by pandas (read/write *.parquet)
python-dateutil==2.9.0.post0
python-dotenv==1.2.2         # common.config reads .env
psycopg2-binary==2.9.12      # Postgres driver (dashboard.data.db + storage)
sqlalchemy==2.0.49           # currently unused in code; kept to preserve prior env
# Must stay <9 to match the Elasticsearch 8.11 server (docker-compose.yml).
# Used by dashboard.data.es AND the pipeline storage step.
elasticsearch==8.19.3
PyYAML>=6.0,<7               # dataset profile loader (common/service_profile.py)

